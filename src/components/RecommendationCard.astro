---
const {stars, name} = Astro.props
---

<article 
    class="h-12 overflow-hidden flex items-center
    bg-white px-4 py-2 rounded-full gap-3 font-semibold
">
    <img class="h-8 w-8 object-cover object-center rounded-full" src={`/img/users/${name}.webp`} alt={`${name}-image`} />
    <div>
        
    </div>
    <span>{name}</span>
</article>