---
const {title, content, cover_name} = Astro.props
---


<div class="h-96 w-72 rounded-xl bg-white overflow-hidden
    hover:rotate-6 transition-transform
">
    <div class="overflow-hidden h-42">
        <img class="hover:scale-110 transition-transform" src={`/img/covers/${cover_name}.webp`} alt={`${title}-cover`}>
    </div>
    <div class="px-4 py-2 space-y-3">
        <h2 class="text-xl font-semibold">{title}</h2>
        <p>
            {content.length > 120 ? content.slice(0, 120) + "..." : content}
        </p>
    </div>
</div>